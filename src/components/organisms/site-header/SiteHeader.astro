---
// src/components/organisms/site-header/SiteHeader.astro
import Logo from '../../atoms/logo/Logo.astro';
import Button from '../../atoms/button/Button.astro';
import HamburgerButton from './HamburgerButton.tsx';
import GlobalNavMenu from './GlobalNavMenu.tsx';
import { globalNavigationData } from '../../../data/NavigationData.ts';
---
{/*
  TODO: Implement Visual Identity and Interactive Enhancements for Site Header:
  - Apply brand color palette:
    - Header background (consider a subtle tint or opacity from the brand palette).
    - Text colors for navigation links, button.
    - Hover/active states for navigation links and button.
  - Apply Swiss-style typography:
    - Ensure font-family for navigation links and button labels uses brand fonts aligned with Swiss principles (clarity, structure).
    - Adjust font weights and sizes for optimal readability.
  - Adjust spacing and layout for a minimalist feel:
    - Increase horizontal and vertical padding within the header.
    - Ensure consistent spacing between logo, navigation, and button.
    - Refine the layout for different screen sizes to maintain a clean look.
  - Add subtle transitions:
    - Smooth transition for header background/shadow changes (e.g., on scroll).
    - Smooth transitions for navigation link hover/active states.
    - Smooth transition for mobile menu open/close (currently has a basic one, refine easing).
  - Implement animations (potentially with GSAP):
    - Consider a subtle animation for the header shrinking or changing appearance on scroll (professional, aware masa depan).
    - Explore animations for menu items appearing/fading in the mobile overlay.
  - Review/Implement Accessibility:
    - Ensure sufficient color contrast for text and interactive elements.
    - Verify keyboard navigation is intuitive for both desktop and mobile menus.
    - Add appropriate ARIA attributes where needed.
  - Reflect the 'kampus perubahan' feel:
    - Could be subtly conveyed through smooth, responsive interactions and a forward-looking clean design.

  Refer to src/assets/components/header/navigation/ CSS files (e.g., nav.pcss) and TSX components (HamburgerButton.tsx, GlobalNavMenu.tsx) for full implementation.
*/}
<header class="sticky top-0 bg-white/80 backdrop-blur-lg shadow-sm z-50">
    <div class="container mx-auto px-6 flex items-center justify-between h-20">
        <div class="flex-shrink-0">
            <a href="/" aria-label="Beranda STIE Dwimulya">
                {/* TODO: Ensure Logo component uses the correct brand logo asset and adheres to brand guidelines for size and clear space. */}
                <Logo src="/logo-primary.svg" alt="STIE Dwimulya Logo" />
            </a>
        </div>

        {/* Desktop Navigation */}
        <nav class="hidden md:flex items-center">
            {/* TODO: Implement styling for GlobalNavMenu (in GlobalNavMenu.tsx and related CSS) including Swiss-style typography, brand colors for links and states, and subtle hover/dropdown transitions. */}
            <GlobalNavMenu data={globalNavigationData} />
        </nav>

        {/* Call to Action Button */}
        <div class="hidden md:block">
            {/* TODO: Ensure Button component styling aligns with brand guidelines (colors, typography, states, subtle micro-interactions). */}
            <Button href="/penerimaan" label="Pendaftaran" />
        </div>

        {/* Hamburger Button for Mobile */}
        <div class="md:hidden">
            {/* TODO: Style the HamburgerButton (in HamburgerButton.tsx and related CSS) to fit the minimalist design. Add a clear, possibly animated, state change for open/close. */}
            <HamburgerButton client:load />
        </div>
    </div>
</header>

{/* Mobile Navigation Panel (controlled by HamburgerButton) */}
{/* TODO: Refine the mobile menu overlay styling (in related CSS) for a cleaner look. Adjust padding/spacing. Ensure smooth open/close transition (using CSS or GSAP). Style the navigation links and button within the overlay consistent with desktop, but optimized for mobile. */}
<div id="mobile-menu-overlay" class="fixed top-0 left-0 w-full h-full bg-white transform -translate-x-full transition-transform duration-300 ease-in-out z-40 md:hidden">
    <div class="p-6 pt-24">
        <nav>
            <GlobalNavMenu data={globalNavigationData} isMobile={true} />
        </nav>
        <div class="mt-8">
            <Button href="/penerimaan" label="Pendaftaran" />
        </div>
    </div>
</div>

